#include <iostream>
#include <string>

using namespace std;

int main()
{
    int n;
    int proverka;
    setlocale(LC_ALL, "Russian");
    cout << "Как будем формировать список фамилий?(1 - вручную, 2 - готовый)";
    cin >> proverka;
    switch(proverka)
    {
        /*case 1:
            cout << "Сколько вводите фамилий? "<< endl;
            cin >> n;
    
            for (int i = 0; i<=n; i++)
            {
                cout << "Введите фамилию: " << endl;
                cin >> A[i]; 
            }
            
            int i, j, step;
            int temp2;//временная переменная
            for (step = n / 2; step > 0; step = step / 2)
            {
                for (i = step; i < n; i++)
                {
                    temp2 = A[i];
                    for (j = i; j >= step; j = j - step)
                    {
                        if (temp2 < A[j - step])
                        {
                            A[j] = A[j - step];
                        }
                        else break;
                    }
                    A[j] = temp2;
                }
            }
            for (i = 0; i < n; i++)
            {
                cout<<A[i]<<" ";
            }
            break;*/
        case 2: //Это должно было работать, но error: cannot convert ‘’ to ‘char’ in assignment - не хочет этого явно :( 
            n = 16;
            string A[n] = {"Мерекин", "Малков", "Семенов", "Рассохина", "Попов", "Фуртатов","Бахарева", "Куницына", "Магомедрагимова", "Писарев", "Ершов", "Белевская", "Воропаев", "Луканина", "Каримов", "Шапневский"};
            int i, j, step;
            int temp2;//временная переменная
            for (step = n / 2; step > 0; step = step / 2)
            {
                for (i = step; i < n; i++)
                {
                    temp2 = A[i];
                    for (j = i; j >= step; j = j - step)
                    {
                        if (temp2 < A[j - step])
                        {
                            A[j] = A[j - step];
                        }
                        else break;
                    }
                    A[j] = temp2;
                }
            }
            for (i = 0; i < n; i++)
            {
                cout<<A[i]<<" ";
            }
            break;
    }
}
/*void SortShella(char** A, int n) // что-то не получилось с отдельной функцией, так что получилась copy-paste в коде. Извините(
{
    int i, j, step;
    char tmp;
    for (step = n / 2; step > 0; step /= 2)
    {
        for (i = step; i < n; i++)
        {
            tmp = A[i];
            for (j = i; j >= step; j -= step)
            {
                if (tmp < A[j - step])
                    A[j] = A[j - step];
                else
                    break;
            }
            A[j] = tmp;
        }
    }
    for (i=0; i<n; i++) 
    {
        cout<<A[i]<<" "; //вывод массива
    }
}*/

